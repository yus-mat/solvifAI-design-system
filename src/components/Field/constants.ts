/**
 * Default max length for single-line text inputs.
 * 255 is the common VARCHAR(255) / general-purpose text field limit on the web.
 */
export const INPUT_MAX_LENGTH = 255;

export const INPUT_MAX_LENGTH_ERROR =
  '255文字以内で入力してください';
